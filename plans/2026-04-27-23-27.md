CONCORD - New Integration

Reticulum - a mesh network standard that is already operational today and fulfills many of the design requirements of concord-beta's mesh network.

- regular concord should natively support reticulum. *(routed 2026-04-13 → concord INS-037)*

- concord-beta will attempt to iterate on reticulum by integrating the mesh map

- concord-beta is about finding out if a procedurally generated map of mesh activity can be built structurally into a networking protocol

- concord beta is about finding out if a wireguard tunnel is enough to connect to nodes from across the world and initiate a video call. 

- It would need to be able to connect multiple nodes together in this way for conference calling. 

- Reticulum wil be the maps disvocery mechanism. The mesh-map will be peoples secure comms mechanism. So nodes can see other nearby nodes viw reticulum, then they can tunnel to those nodes via wireguard+headscale where they use the mesh-map to work together with the other nodes to create a stable conferencable video chat that is co-hosted by the participants and completely secure. 

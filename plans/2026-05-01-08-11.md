ORRAPUS - orragen notes


- the queued imaged never display in the oprragen image generation page. They just way queued forever. i know they are working because the gallery populates with the genreation results but the image gen page doesnt. This is likely because of an oldf bug fix which attemtped to address files that were immunge to delete and wouldnt allow me to remove them from the system. These images remaine4d on dsisp[lay after many attempts to rewmove the. This is unaccapteable. BUT displaying the images as their created is not. the orragen page should be populated with the 4 most recent batches but there should be a button that clear the histroy and erases all of the images that dispolay on thei genrate/timage previwe pane

- The "submitting..." disp[lay needs to go away. The job submits really quikcly. 

- What we need to see is a job queue that is clearly managed and demarked that allows the user to send sets of parameters to a buffer that will submit them as commands when the image generation instance becomes available. I want to be able t queue uyp more than 25 jobs and see all of then simple and neavigably interactable in the session history on the generate/image page.

- If I am on the genrate/image page I expect that the generate page be highlighted as well. if this is not currently the case make it so. 

- this session history needs to be absolutely comprehensive. if a use hasnt deleted a file then it is presen in the history no matter how old 

- orrapu susers always have FULL access to delete things on the connected omputerss

- test interactions need to have a similar session history

- the generate image profiles stopped remembering prompt content. No matter what profile I choose in the generate image tab, the positivea nd negative prompts staye xactly the same


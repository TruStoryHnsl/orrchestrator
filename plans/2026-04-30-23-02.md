CONCORD - Mobile UI Refresh

MOBILE UI NOTES (small display ui)

- The sources page is inaccessible right now. There is not a panel to the left of the servers panel

- I thought oyu fixeed this but maybe it was regressed. Local server tiles default above the + tile not below it. Only bridged or matrix servers default under the + tile (for sources too). Other concord instances also default to above the + tile. Okay so Concord servers default above the + tile and non-concord tiles default above. 

- move the settings menu oput of the pill menu and up to the top menu bar instead. This

- get rid of the quick actions tab, its not useful.

- the pill menu needs to be hideable. It should operate like a dock in any other operating system. A swipe up reveals it and lets you move betweenthings and then a swipe down hides it again. A click needs to be suppurted too. 

- most importantle the pill buttons NEVER swipe the page from left or right. The settings page needs to be its own thing not accessible with swiping anymore. 

- The user can swipe through all of these pages starting from the left with: sources, servers, channels, room. The pill buttons save the navigation so when the oser goes back to the browse display they go right back to where they were. If they poress the + they will open a fresh browse starting at the welcome page. 

- add a + pill to the pill menu that opens a parallel instance of the server browser. This way users can navigate to a channel and then press the + button to open a fresh instance and navigate to another chat. Once they have a few of this up they can see notifications from these pills and quickly switch between the different rooms the user is participating in. This + pill should be on the left side of the menu.  so after the two changes to the pill menu the user should see, on a fresh instance, three items in the pill menu. from left to right its +, browse, DM. If the user creates more browse tabs with the plus button the pill menu will dynamically adjust to accomodate them within the window parameters. 

- I want the tiles from a screen to the left of the current screen to be expandable from the left side of the screen. Meaning If am on the channels page i should be able to tap/click on the left side of the main display panel to show the server tiles. If Im in a channel chatting I can tap the left side of the screen and see the channels available on this server displayed as tiles. 

- additionaly I want tapping the right side of a page to display a shortcut menu for that page also. so if I was on the servers menu I could tap on the right side of the screen to show the channels depicted as tiles. 

- all of these collapsable quickmenu's need to display as tiles because they cant take up very much space on the page. 

- whenever I navigate to the browse tab from any other page the manu moves all the way to the left and then races to the right to get into the channel I was most recetly in. This is terrible. Either I am still on that page on that browse tab or I am not, I dont wnat that autonavigation garbge. When I click the back arrow or swipe left on the settings menu I should be taken to the menu I was looking at just before I opened the settings menu. 

- The status bar should change color depending on different states the hardware is in. For example if the microphone is capturing audio the status bar should be orange. if it is capturing video the status bar should be green. If it is capturing those things but not broadcasting them anywhere then the banner should have a bright blue border around it. This indicates that the browser is reading the camera or microhpnes input but it isnt connected to a room and isnt doing anything with that connection. THese kinds of idle connections are bad we want to get rid of them as much as possible and this will help me identify them. 

- The webui needs to have the servitude UI built into it. The user accessing via the web is interacting essentially the same way as with the native ui. Servitude needs to be recognized as the "host" of concord instances while concord itself is just a client. Even though the docker build is more trnsparently a server, It should operate the exact same way as the native apps. So that means that 

- All hosting settings or services need to be routed throught eh servitude UI portal. When a user first boots up a fresh concord instance via docker they should be asked host/join and if they choose hose then concord will say "starting servitude" and then ask the user to confirm the domain they would like servitude to serve on. This is set by the install.sh script so most users will just click yes. 

- On native its not a given that a user has a domain so I will, for now, use a domain that I own called concordchat.net as the root domain for any user who doesnt have their own domain. This means that there will be a lot of *.concordchat.net servers out there. Thats okay I guess. this concordchat.net domain can be backed into the concord package as the default domain root for a new server. 

- The native users should be able host a fully capable concord server right in the app without worrying about docker or any of that noise. If a user wants to let their laptop idle all the time and keep the desktop application running then anyone can use their server if they are invited. 

- It needs to be possible for users to create channels on servers they've been added to. This probably already exists I just need to promote my friends to admins so they can create channels if they want to. 

- I want users on a server to be able to create "private" channels (if permitted by the admin) that they can go into to chat with only whitelisted users. Basically I want users to feel like they have privacy from the admin. 

- Concord is infrastructure. Users need to have their privacy protected from the admin as well. I geuss they have DM's that the admin cant see. That fulfills this feature request entirely. Oh whatever, maybe we dont need this or the previous thing. You tell me. 

- 

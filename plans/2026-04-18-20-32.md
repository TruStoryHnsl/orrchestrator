Orrapus Update - FORK
+ GLSR Generic Live Stream Recorder

This add functionality to monitor many other streaming websites in the same fashion with which we monitor chaturbate. 

Our favorites list will not be seperated by platform but the experience for the end user will be identical. They can submit a link and the live stream recorder will add that url to the list to be monitored for live status. 

I want all of the stream recording logic to be made generic. It all still needs to work with chaturbate thte way that it has, but other platforms should he supported too. I want the logic to be so versatile and so robust that GLSR is able to monitor twitch streams, chaturbate shows, or any other periodic stream all the same.   

---

## Intake resolution (2026-04-07)

**Already covered.** Routed through the orrchestrator intake pipeline 2026-04-07. The COO optimized this into 8 OPT items, but on review the work is **fully covered by the existing omnipus plan** — the entire idea has already been turned into a detailed Phase 1–5 implementation roadmap inside `~/projects/omnipus/PLAN.md` and `~/projects/omnipus/instructions_inbox.md` (omnipus INS-001 through INS-015).

**Key clarification (user, 2026-04-07)**:
- omnipus is the **repo-level fork of orrapus**. cbsr lives in orrapus (untouched). glsr lives in omnipus and replaces omnipus's inherited cbsr. orrapus is never touched when working on omnipus.
- glsr is the Rust replacement for cbsr; "fork" in this idea means *omnipus = fork of orrapus*, not "fork of orrapus into a new GLSR repo".

**Mapping new OPTs → existing omnipus INS items** (no new INS items needed):
| New OPT | Already covered by omnipus |
|---------|----------------------------|
| OPT-001 (fork orrapus into GLSR) | omnipus exists; INS-001 (Cargo workspace, glsr lib + glsr-cli bin) |
| OPT-002 (generalize stream recording core) | INS-002 (`Platform` trait), P1-03..06 |
| OPT-003 (multi-platform adapters) | INS-003 (Chaturbate), INS-004 (Twitch), INS-005 (generic yt-dlp fallback) |
| OPT-004 (unified favorites list) | INS-006 (flat platform-agnostic favorites), P4-06..07 |
| OPT-005 (URL submission as sole entry point) | INS-007 (URL-based detection), INS-011 (`glsr <url>` CLI), P2-10..11 |
| OPT-006 (preserve cbsr behavioral parity) | INS-003 ("Zero regression. same API endpoint, same user-agent, same timeouts"), P2-04 |
| OPT-007 (robust adapter framework) | INS-007 fallback chain, INS-010 monitor error handling/retry, INS-013 logging, INS-015 commercial polish |
| OPT-008 (staging) | omnipus PLAN.md Phase 1 → Phase 5 already sequences this |

**Action taken**: orrapus left untouched. No new INS items added to omnipus (existing plan already complete). Idea closed as "already covered". Status: **complete (covered by existing plan)** — no further routing required.

**Pipeline workspace**: `~/projects/orrchestrator/plans/.pipeline/2026-04-18-20-32/`

ORRCHESTRATOR - Feedback 

- When a new file is beign created by the ideation page the desktop focus should move to the new window so the user can immediately start typing. 

- There is both a dev map and a feaature road map on all of the project detail pages. Only show the roadmap. 

- Also this raodmap isnt scrollable and it should be. The structure we built to ensure that overflowed content is always scrolled missed this one. That cant be happening. 

- The project menu traps the user. I cant navigate up or back to exit. Test all pages for this behavior and fix any that display it. 

- pressing right arrow on a project displays a deprecated project view. it needs to display the project details. ANd the project details need to be exitable with a left-arrow. 

- I still see projects whose tasks arent tracked. I started building borrk today but its dev completeion isnt being shown on the oversee menu. 

- When a project has completed all of its planned features the tip line should say 'submit feedback' and 'construct packages'

- We need to have a special complex and detiled workflow for porting projects onto whatever platform they want. I have ideas for how to lay this out. You take a shot at it and I will come around and change/replace the workflow you write. 

- We need to add a set of skills to orrchestrators mcp that allow the agent to create new skills, agents, tools, workflows, etc on the fly. If it encounters a new problem or confusion it sould ask if there is an expert that might know better. Then the agent should create that expert agent and any skills that agent might need, save that agent to the library, and then use the newly created abolities to solve the given problem.

- Projects that have a workflow run ont hem should be moved to the hot section of the projects view. 

- If a project is still open on the desktop or in tmux then the project should stay hot. But the timer starts whent he window goes idle. That way they immediately go cold if the user closes the stale window keeping them hot. Rather than the timer starting right then. 

- There seems to be no logic for closing stale or unneeded windows. auto-commit processes should not sit stale after they are finished. Same goes for workflows. 

- We need to add an instruction to the agents that says whenever a workflow is fully complete and the sessions work has been committed a key phrase will be printed. That key phrase is orrchestrators trigger to close the open session. 

- If a sessions is waitng for the user to answer a question or if it has an issue to report then orrchestrator should also know to indicate that to the user in the orrchestrator ui. 

- To close each of these sessions the agent will do this proces:
    - verify that the work has been committed and pushed. 
    - run the mempalace command to remember the session.
    - write a session brief to the projects orrchestrator data. 
    - These session briefs need to be readable and navigable in the project details menu.  

- orrchestrator should use nvim instead of vim if it isnt already. 

- The pane labels in tmux are god awful. The tab names are way too long and the project their associated with gets buried under useless information. 

- I want to be able to split orrchestrator tmux windows off. I want to be able to show two orrchestrator tmux windows at once by spitting off one tab into its own window. It seems that something is blocking me from doing this right now. 

- There need to be clear labels in the tmux config and oprrchestrator should be imposing its own config onto tmux rather than using the system config. That means orrchestrator needs to have working config files for all operating systems it intend to open tmux windows on and apply them appropriately. 

- There need to be robust project classification tools. I need to be able to take projects out of admin and move them into the regular projects and vice versa. I need to be able to explore the deprecated folder and similarly move things ina nd out. I need to be able to delete things in the deprecated folder. 

- in general orrchestrator needs to be a more capable file manager. I want to be able to rename files, projects, intentions, plans, whatever without ever leaving orrchestrator

- write the plan for the Publish page and add it to orrchestrators plan.md. We need to get to work building that ASAP.

- The alanyze page also is not what it is supposed to be. We need to flush that out with many more metrics. Ask teh ressearcher to help you decide what kinds of metrics are worth tracking. 

- Thje hypervise tab barely works. This is what it needs to be able to do:
    - rename sessions 
    - expand sessions to display the most two most recent messages 
    - drill into sessions to a sessions detail page which contains:
        - live display of the messages coming in from the session. 
        - a text box in which to write prompts (vim)
        - a dashboard containing workforces, workflows, skills, tools, mcp servers, etc. 
        - the ability to invoke these workflows or skills via this dashboard
        - interrupt the sessions processes
    - display the sessions host machine
    - display the sessions working directory
    - clear and restart sessions with an edited spawning prompt







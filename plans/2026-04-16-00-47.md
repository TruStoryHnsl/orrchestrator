ORRCHESTRATOR - FEEDBACK

> **Batch complete: 9/9 instructions implemented (2026-04-03 → 2026-04-04)**
> INS-001 responsive tab bar, INS-002 workforce nav fix, INS-003 library justify,
> INS-004 harnesses tab, INS-005 markdown preview, INS-006 tmux cleanup,
> INS-007 custom status bar, INS-008 hub vim window, INS-009 audit trail.
> See DEVLOG.md for implementation details and known issues.

 - The minimum width of the title bar is too wide. The 5 tabs are extending beyond the window. They need to be
  compressable down to abbreviated title displays or ven no title displays, just blank tabs with a colored selector.
  Even beter, when

  - The workforce submenu is not selectable. The cursor skips past it and the left to right arrows only move between
  the first submenu. The second row of submenus is not navigable.

  - I dont like my choice to move the library to the right. move it back to be justified with the other two.

  - Harnesses was not on the design list, but we do need to add it as a design page. Harnesses are even higher level
  than the workflows/workforces. The harness should now be the leftmost panel and it should focus a harness creation
  workflow that reads the source code for these harnesses to create a catalogue of modules, teams, workflows, tools,
  whatever the harness has built in. This editor would allow me to scrape the operational logic from existing
  harnesses and hotswap them into my planned custom harness forked from the rust rewrite of claude code claw code. I
  intend to automate the development of my forked version given the various featuresets of the competing harnesses. I
  have a raw copy of all of the harnesses we use because they are all open source now that claude code was leaked. I
  intend to smash them all together, index their features, document their theoretical mechaisms, create my own feature
  paritous code (or just literally take code from the others, I know they do it to each other) that I can modify live
  in an abstracted visual coding language powered by flexible logic units made up of teams of ai agents. The harness
  aggregator will create feature paritous modules, nest them insode other modules to create better modules and               (   )
  generally just procedurally build denser and denser logic frameworks with more and more layers of segmentaion and          .----.
  abstraction. ---- Any, I got excited there. I want to *eventually* have an editor of this kind available in               ( ×  × )
  orrchestrator but for now all we need is the menu placeholder.                                                        


- we need to create tui rendering layer that can be embedded into a tui window, scrolled seperately to the rest of the terminal and displaying an image rendering protocol that displays .md files with full rich formatting display. This rendering layer would be utilized to render rich text formatting for .md files in preview panes as well as in neovim instances. before you go too far on this, see if implementationes exist that you can steal. The image embedding program that works for yazi is a great place to start if you need inspiration. Thats another tui that displays a clean image. We need to do that also but have the image be identical to the vim display but with rich formatting displayed instead of the perfectly metered terminal-limited display. 

- orrchestrator is not exiting tmux sessions properly. There are a dozen orphaned tmux sessions. Please fix this so it doesnt keep happening. 

- while you are looking at tmux, I want to use a custom status bar for tmux windows managed by orrchestrator. This display will just provide a tab navigation experience that is more rish than tmux provides natively. I want to track each tmux windows status wether they are busy, waiting, or idle. Then it lists the windows in order of urgency where the windows that need attention most are listed closest to the users origin point. Then I want to create a custom hotkey that navigates that reads the sessions states and opens the most urgent one. pair that with the built in tmux hotkey that switches to the next session on the list and the user suddenly has very efficent navigation between the layered displays of claude sessions.


- vim/nvim text editing sessions should all be opened in one tmux window. With the rich custom status bar i described above implemented for this window as well, the user can navigate the independant tmux+vim window as if its a native orrchestrator window. If the user splits a vim session off into its own window, orrchestrator tracks the change and represents the lone window in the ideas menu. 

- I dont think I specified until now but I want ideas logged in the design intentions menu in a similar fashion to the projects. They should have a cycleable colored tag available that I can use for miscellaneous sorting. I want a record of all the design documents I write that such that I can navigate an intermingled tree of ideas that were written into the same file but belonged to different projects. I want my feedback, which is often targetting multiple apps, to create a user contributions log. This log will exist in every orrchstrator project and it will record all of the instructions that are written to the project. The COO, when he wrote the instructions and split them up into deperate packages, indexed the instructions witha hash that describes the coordinate data of the chunk of text that was optimized to create the instruction. This way I have a record that shows how my prompts are being inperpreted. I want to be able to cleanly audit this translation layer as it has the most potential to distory my intentions.


